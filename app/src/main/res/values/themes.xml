<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Applant" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Applant" parent="Base.Theme.Applant" />

    <style name="CustomAlertDialog" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <!-- Kolor tła -->
        <item name="android:background">@color/menu</item>
        <!-- Kolor tekstu -->
        <item name="android:textColor">@color/text</item>
        <!-- Kolor przycisków -->
        <item name="colorAccent">@color/purple</item>

        <item name="android:textAllCaps">false</item>
        <item name="android:letterSpacing">0</item>
    </style>

    <style name="CustomAlertDialogPlants" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <!-- Kolor tła -->
        <item name="android:background">@color/item</item>
        <!-- Kolor tekstu -->
        <item name="android:textColor">@color/text</item>
        <!-- Kolor przycisków -->
        <item name="colorAccent">@color/purple</item>

        <item name="android:textAllCaps">false</item>
        <item name="android:letterSpacing">0</item>
    </style>

    <style name="CustomDatePickerDialog" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">

        <item name="android:background">@color/menu</item>
        <item name="android:colorControlActivated">@color/purple</item>
    </style>

</resources>